#include "nntree/optimizer.h"
#include "nntree/dataset.h"
#include "nntree/cost_function.h"

namespace nntree {
namespace core {

void Optimizer::Step() {
//  auto batch = ds_->RandomBatch(100);
//  auto f = loss_->Backward(batch);
//  auto weights_changes = model_->Backward(f);
//  model_->AdjustParams(weights_changes);
}

}
}
